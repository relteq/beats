package com.relteq.sirius.om;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Date;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;





/**
 * This class was autogenerated by Torque on:
 *
 * [Wed Nov 14 14:25:09 PST 2012]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to Routes
 */
public abstract class BaseRoutes extends com.relteq.sirius.db.BaseObject
{
    /** Serial version */
    private static final long serialVersionUID = 1352931909099L;

    /** The Peer class */
    private static final RoutesPeer peer =
        new RoutesPeer();


    /** The value for the id field */
    private Long id;

    /** The value for the projectId field */
    private Long projectId;

    /** The value for the name field */
    private String name;

    /** The value for the created field */
    private Date created;

    /** The value for the modified field */
    private Date modified;

    /** The value for the createdBy field */
    private String createdBy;

    /** The value for the modifiedBy field */
    private String modifiedBy;

    /** The value for the modstamp field */
    private Date modstamp;


    /**
     * Get the Id
     *
     * @return Long
     */
    public Long getId()
    {
        return id;
    }


    /**
     * Set the value of Id
     *
     * @param v new value
     */
    public void setId(Long v) throws TorqueException
    {

        if (!ObjectUtils.equals(this.id, v))
        {
            this.id = v;
            setModified(true);
        }



        // update associated RouteLinks
        if (collRouteLinkss != null)
        {
            for (int i = 0; i < collRouteLinkss.size(); i++)
            {
                ((RouteLinks) collRouteLinkss.get(i))
                        .setRouteId(v);
            }
        }

        // update associated RouteSets
        if (collRouteSetss != null)
        {
            for (int i = 0; i < collRouteSetss.size(); i++)
            {
                ((RouteSets) collRouteSetss.get(i))
                        .setRouteId(v);
            }
        }

        // update associated RoutePerformanceTotal
        if (collRoutePerformanceTotals != null)
        {
            for (int i = 0; i < collRoutePerformanceTotals.size(); i++)
            {
                ((RoutePerformanceTotal) collRoutePerformanceTotals.get(i))
                        .setRouteId(v);
            }
        }
    }

    /**
     * Get the ProjectId
     *
     * @return Long
     */
    public Long getProjectId()
    {
        return projectId;
    }


    /**
     * Set the value of ProjectId
     *
     * @param v new value
     */
    public void setProjectId(Long v) throws TorqueException
    {

        if (!ObjectUtils.equals(this.projectId, v))
        {
            this.projectId = v;
            setModified(true);
        }


        if (aProjects != null && !ObjectUtils.equals(aProjects.getId(), v))
        {
            aProjects = null;
        }

    }

    /**
     * Get the Name
     *
     * @return String
     */
    public String getName()
    {
        return name;
    }


    /**
     * Set the value of Name
     *
     * @param v new value
     */
    public void setName(String v) 
    {

        if (!ObjectUtils.equals(this.name, v))
        {
            this.name = v;
            setModified(true);
        }


    }

    /**
     * Get the Created
     *
     * @return Date
     */
    public Date getCreated()
    {
        return created;
    }


    /**
     * Set the value of Created
     *
     * @param v new value
     */
    public void setCreated(Date v) 
    {

        if (!ObjectUtils.equals(this.created, v))
        {
            this.created = v;
            setModified(true);
        }


    }

    /**
     * Get the Modified
     *
     * @return Date
     */
    public Date getModified()
    {
        return modified;
    }


    /**
     * Set the value of Modified
     *
     * @param v new value
     */
    public void setModified(Date v) 
    {

        if (!ObjectUtils.equals(this.modified, v))
        {
            this.modified = v;
            setModified(true);
        }


    }

    /**
     * Get the CreatedBy
     *
     * @return String
     */
    public String getCreatedBy()
    {
        return createdBy;
    }


    /**
     * Set the value of CreatedBy
     *
     * @param v new value
     */
    public void setCreatedBy(String v) 
    {

        if (!ObjectUtils.equals(this.createdBy, v))
        {
            this.createdBy = v;
            setModified(true);
        }


    }

    /**
     * Get the ModifiedBy
     *
     * @return String
     */
    public String getModifiedBy()
    {
        return modifiedBy;
    }


    /**
     * Set the value of ModifiedBy
     *
     * @param v new value
     */
    public void setModifiedBy(String v) 
    {

        if (!ObjectUtils.equals(this.modifiedBy, v))
        {
            this.modifiedBy = v;
            setModified(true);
        }


    }

    /**
     * Get the Modstamp
     *
     * @return Date
     */
    public Date getModstamp()
    {
        return modstamp;
    }


    /**
     * Set the value of Modstamp
     *
     * @param v new value
     */
    public void setModstamp(Date v) 
    {

        if (!ObjectUtils.equals(this.modstamp, v))
        {
            this.modstamp = v;
            setModified(true);
        }


    }

    



    private Projects aProjects;

    /**
     * Declares an association between this object and a Projects object
     *
     * @param v Projects
     * @throws TorqueException
     */
    public void setProjects(Projects v) throws TorqueException
    {
        if (v == null)
        {
            setProjectId((Long) null);
        }
        else
        {
            setProjectId(v.getId());
        }
        aProjects = v;
    }


    /**
     * Returns the associated Projects object.
     * If it was not retrieved before, the object is retrieved from
     * the database
     *
     * @return the associated Projects object
     * @throws TorqueException
     */
    public Projects getProjects()
        throws TorqueException
    {
        if (aProjects == null && (!ObjectUtils.equals(this.projectId, null)))
        {
            aProjects = ProjectsPeer.retrieveByPK(SimpleKey.keyFor(this.projectId));
        }
        return aProjects;
    }

    /**
     * Return the associated Projects object
     * If it was not retrieved before, the object is retrieved from
     * the database using the passed connection
     *
     * @param connection the connection used to retrieve the associated object
     *        from the database, if it was not retrieved before
     * @return the associated Projects object
     * @throws TorqueException
     */
    public Projects getProjects(Connection connection)
        throws TorqueException
    {
        if (aProjects == null && (!ObjectUtils.equals(this.projectId, null)))
        {
            aProjects = ProjectsPeer.retrieveByPK(SimpleKey.keyFor(this.projectId), connection);
        }
        return aProjects;
    }

    /**
     * Provides convenient way to set a relationship based on a
     * ObjectKey, for example
     * <code>bar.setFooKey(foo.getPrimaryKey())</code>
     *
     */
    public void setProjectsKey(ObjectKey key) throws TorqueException
    {

        setProjectId(new Long(((NumberKey) key).longValue()));
    }
   


    /**
     * Collection to store aggregation of collRouteLinkss
     */
    protected List collRouteLinkss;

    /**
     * Temporary storage of collRouteLinkss to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initRouteLinkss()
    {
        if (collRouteLinkss == null)
        {
            collRouteLinkss = new ArrayList();
        }
    }


    /**
     * Method called to associate a RouteLinks object to this object
     * through the RouteLinks foreign key attribute
     *
     * @param l RouteLinks
     * @throws TorqueException
     */
    public void addRouteLinks(RouteLinks l) throws TorqueException
    {
        getRouteLinkss().add(l);
        l.setRoutes((Routes) this);
    }

    /**
     * Method called to associate a RouteLinks object to this object
     * through the RouteLinks foreign key attribute using connection.
     *
     * @param l RouteLinks
     * @throws TorqueException
     */
    public void addRouteLinks(RouteLinks l, Connection con) throws TorqueException
    {
        getRouteLinkss(con).add(l);
        l.setRoutes((Routes) this);
    }

    /**
     * The criteria used to select the current contents of collRouteLinkss
     */
    private Criteria lastRouteLinkssCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getRouteLinkss(new Criteria())
     *
     * @return the collection of associated objects
     * @throws TorqueException
     */
    public List getRouteLinkss()
        throws TorqueException
    {
        if (collRouteLinkss == null)
        {
            collRouteLinkss = getRouteLinkss(new Criteria(10));
        }
        return collRouteLinkss;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes has previously
     * been saved, it will retrieve related RouteLinkss from storage.
     * If this Routes is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getRouteLinkss(Criteria criteria) throws TorqueException
    {
        if (collRouteLinkss == null)
        {
            if (isNew())
            {
               collRouteLinkss = new ArrayList();
            }
            else
            {
                criteria.add(RouteLinksPeer.ROUTE_ID, getId() );
                collRouteLinkss = RouteLinksPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                criteria.add(RouteLinksPeer.ROUTE_ID, getId());
                if (!lastRouteLinkssCriteria.equals(criteria))
                {
                    collRouteLinkss = RouteLinksPeer.doSelect(criteria);
                }
            }
        }
        lastRouteLinkssCriteria = criteria;

        return collRouteLinkss;
    }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getRouteLinkss(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getRouteLinkss(Connection con) throws TorqueException
    {
        if (collRouteLinkss == null)
        {
            collRouteLinkss = getRouteLinkss(new Criteria(10), con);
        }
        return collRouteLinkss;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes has previously
     * been saved, it will retrieve related RouteLinkss from storage.
     * If this Routes is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getRouteLinkss(Criteria criteria, Connection con)
            throws TorqueException
    {
        if (collRouteLinkss == null)
        {
            if (isNew())
            {
               collRouteLinkss = new ArrayList();
            }
            else
            {
                 criteria.add(RouteLinksPeer.ROUTE_ID, getId());
                 collRouteLinkss = RouteLinksPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                 criteria.add(RouteLinksPeer.ROUTE_ID, getId());
                 if (!lastRouteLinkssCriteria.equals(criteria))
                 {
                     collRouteLinkss = RouteLinksPeer.doSelect(criteria, con);
                 }
             }
         }
         lastRouteLinkssCriteria = criteria;

         return collRouteLinkss;
     }











    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes is new, it will return
     * an empty collection; or if this Routes has previously
     * been saved, it will retrieve related RouteLinkss from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Routes.
     */
    protected List getRouteLinkssJoinRoutes(Criteria criteria)
        throws TorqueException
    {
        if (collRouteLinkss == null)
        {
            if (isNew())
            {
               collRouteLinkss = new ArrayList();
            }
            else
            {
                criteria.add(RouteLinksPeer.ROUTE_ID, getId());
                collRouteLinkss = RouteLinksPeer.doSelectJoinRoutes(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(RouteLinksPeer.ROUTE_ID, getId());
            if (!lastRouteLinkssCriteria.equals(criteria))
            {
                collRouteLinkss = RouteLinksPeer.doSelectJoinRoutes(criteria);
            }
        }
        lastRouteLinkssCriteria = criteria;

        return collRouteLinkss;
    }









    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes is new, it will return
     * an empty collection; or if this Routes has previously
     * been saved, it will retrieve related RouteLinkss from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Routes.
     */
    protected List getRouteLinkssJoinLinkFamilies(Criteria criteria)
        throws TorqueException
    {
        if (collRouteLinkss == null)
        {
            if (isNew())
            {
               collRouteLinkss = new ArrayList();
            }
            else
            {
                criteria.add(RouteLinksPeer.ROUTE_ID, getId());
                collRouteLinkss = RouteLinksPeer.doSelectJoinLinkFamilies(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(RouteLinksPeer.ROUTE_ID, getId());
            if (!lastRouteLinkssCriteria.equals(criteria))
            {
                collRouteLinkss = RouteLinksPeer.doSelectJoinLinkFamilies(criteria);
            }
        }
        lastRouteLinkssCriteria = criteria;

        return collRouteLinkss;
    }





    /**
     * Collection to store aggregation of collRouteSetss
     */
    protected List collRouteSetss;

    /**
     * Temporary storage of collRouteSetss to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initRouteSetss()
    {
        if (collRouteSetss == null)
        {
            collRouteSetss = new ArrayList();
        }
    }


    /**
     * Method called to associate a RouteSets object to this object
     * through the RouteSets foreign key attribute
     *
     * @param l RouteSets
     * @throws TorqueException
     */
    public void addRouteSets(RouteSets l) throws TorqueException
    {
        getRouteSetss().add(l);
        l.setRoutes((Routes) this);
    }

    /**
     * Method called to associate a RouteSets object to this object
     * through the RouteSets foreign key attribute using connection.
     *
     * @param l RouteSets
     * @throws TorqueException
     */
    public void addRouteSets(RouteSets l, Connection con) throws TorqueException
    {
        getRouteSetss(con).add(l);
        l.setRoutes((Routes) this);
    }

    /**
     * The criteria used to select the current contents of collRouteSetss
     */
    private Criteria lastRouteSetssCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getRouteSetss(new Criteria())
     *
     * @return the collection of associated objects
     * @throws TorqueException
     */
    public List getRouteSetss()
        throws TorqueException
    {
        if (collRouteSetss == null)
        {
            collRouteSetss = getRouteSetss(new Criteria(10));
        }
        return collRouteSetss;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes has previously
     * been saved, it will retrieve related RouteSetss from storage.
     * If this Routes is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getRouteSetss(Criteria criteria) throws TorqueException
    {
        if (collRouteSetss == null)
        {
            if (isNew())
            {
               collRouteSetss = new ArrayList();
            }
            else
            {
                criteria.add(RouteSetsPeer.ROUTE_ID, getId() );
                collRouteSetss = RouteSetsPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                criteria.add(RouteSetsPeer.ROUTE_ID, getId());
                if (!lastRouteSetssCriteria.equals(criteria))
                {
                    collRouteSetss = RouteSetsPeer.doSelect(criteria);
                }
            }
        }
        lastRouteSetssCriteria = criteria;

        return collRouteSetss;
    }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getRouteSetss(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getRouteSetss(Connection con) throws TorqueException
    {
        if (collRouteSetss == null)
        {
            collRouteSetss = getRouteSetss(new Criteria(10), con);
        }
        return collRouteSetss;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes has previously
     * been saved, it will retrieve related RouteSetss from storage.
     * If this Routes is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getRouteSetss(Criteria criteria, Connection con)
            throws TorqueException
    {
        if (collRouteSetss == null)
        {
            if (isNew())
            {
               collRouteSetss = new ArrayList();
            }
            else
            {
                 criteria.add(RouteSetsPeer.ROUTE_ID, getId());
                 collRouteSetss = RouteSetsPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                 criteria.add(RouteSetsPeer.ROUTE_ID, getId());
                 if (!lastRouteSetssCriteria.equals(criteria))
                 {
                     collRouteSetss = RouteSetsPeer.doSelect(criteria, con);
                 }
             }
         }
         lastRouteSetssCriteria = criteria;

         return collRouteSetss;
     }











    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes is new, it will return
     * an empty collection; or if this Routes has previously
     * been saved, it will retrieve related RouteSetss from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Routes.
     */
    protected List getRouteSetssJoinScenarios(Criteria criteria)
        throws TorqueException
    {
        if (collRouteSetss == null)
        {
            if (isNew())
            {
               collRouteSetss = new ArrayList();
            }
            else
            {
                criteria.add(RouteSetsPeer.ROUTE_ID, getId());
                collRouteSetss = RouteSetsPeer.doSelectJoinScenarios(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(RouteSetsPeer.ROUTE_ID, getId());
            if (!lastRouteSetssCriteria.equals(criteria))
            {
                collRouteSetss = RouteSetsPeer.doSelectJoinScenarios(criteria);
            }
        }
        lastRouteSetssCriteria = criteria;

        return collRouteSetss;
    }









    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes is new, it will return
     * an empty collection; or if this Routes has previously
     * been saved, it will retrieve related RouteSetss from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Routes.
     */
    protected List getRouteSetssJoinRoutes(Criteria criteria)
        throws TorqueException
    {
        if (collRouteSetss == null)
        {
            if (isNew())
            {
               collRouteSetss = new ArrayList();
            }
            else
            {
                criteria.add(RouteSetsPeer.ROUTE_ID, getId());
                collRouteSetss = RouteSetsPeer.doSelectJoinRoutes(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(RouteSetsPeer.ROUTE_ID, getId());
            if (!lastRouteSetssCriteria.equals(criteria))
            {
                collRouteSetss = RouteSetsPeer.doSelectJoinRoutes(criteria);
            }
        }
        lastRouteSetssCriteria = criteria;

        return collRouteSetss;
    }





    /**
     * Collection to store aggregation of collRoutePerformanceTotals
     */
    protected List collRoutePerformanceTotals;

    /**
     * Temporary storage of collRoutePerformanceTotals to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initRoutePerformanceTotals()
    {
        if (collRoutePerformanceTotals == null)
        {
            collRoutePerformanceTotals = new ArrayList();
        }
    }


    /**
     * Method called to associate a RoutePerformanceTotal object to this object
     * through the RoutePerformanceTotal foreign key attribute
     *
     * @param l RoutePerformanceTotal
     * @throws TorqueException
     */
    public void addRoutePerformanceTotal(RoutePerformanceTotal l) throws TorqueException
    {
        getRoutePerformanceTotals().add(l);
        l.setRoutes((Routes) this);
    }

    /**
     * Method called to associate a RoutePerformanceTotal object to this object
     * through the RoutePerformanceTotal foreign key attribute using connection.
     *
     * @param l RoutePerformanceTotal
     * @throws TorqueException
     */
    public void addRoutePerformanceTotal(RoutePerformanceTotal l, Connection con) throws TorqueException
    {
        getRoutePerformanceTotals(con).add(l);
        l.setRoutes((Routes) this);
    }

    /**
     * The criteria used to select the current contents of collRoutePerformanceTotals
     */
    private Criteria lastRoutePerformanceTotalsCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getRoutePerformanceTotals(new Criteria())
     *
     * @return the collection of associated objects
     * @throws TorqueException
     */
    public List getRoutePerformanceTotals()
        throws TorqueException
    {
        if (collRoutePerformanceTotals == null)
        {
            collRoutePerformanceTotals = getRoutePerformanceTotals(new Criteria(10));
        }
        return collRoutePerformanceTotals;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes has previously
     * been saved, it will retrieve related RoutePerformanceTotals from storage.
     * If this Routes is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getRoutePerformanceTotals(Criteria criteria) throws TorqueException
    {
        if (collRoutePerformanceTotals == null)
        {
            if (isNew())
            {
               collRoutePerformanceTotals = new ArrayList();
            }
            else
            {
                criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId() );
                collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
                if (!lastRoutePerformanceTotalsCriteria.equals(criteria))
                {
                    collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelect(criteria);
                }
            }
        }
        lastRoutePerformanceTotalsCriteria = criteria;

        return collRoutePerformanceTotals;
    }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getRoutePerformanceTotals(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getRoutePerformanceTotals(Connection con) throws TorqueException
    {
        if (collRoutePerformanceTotals == null)
        {
            collRoutePerformanceTotals = getRoutePerformanceTotals(new Criteria(10), con);
        }
        return collRoutePerformanceTotals;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes has previously
     * been saved, it will retrieve related RoutePerformanceTotals from storage.
     * If this Routes is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getRoutePerformanceTotals(Criteria criteria, Connection con)
            throws TorqueException
    {
        if (collRoutePerformanceTotals == null)
        {
            if (isNew())
            {
               collRoutePerformanceTotals = new ArrayList();
            }
            else
            {
                 criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
                 collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                 criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
                 if (!lastRoutePerformanceTotalsCriteria.equals(criteria))
                 {
                     collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelect(criteria, con);
                 }
             }
         }
         lastRoutePerformanceTotalsCriteria = criteria;

         return collRoutePerformanceTotals;
     }











    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes is new, it will return
     * an empty collection; or if this Routes has previously
     * been saved, it will retrieve related RoutePerformanceTotals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Routes.
     */
    protected List getRoutePerformanceTotalsJoinRoutes(Criteria criteria)
        throws TorqueException
    {
        if (collRoutePerformanceTotals == null)
        {
            if (isNew())
            {
               collRoutePerformanceTotals = new ArrayList();
            }
            else
            {
                criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
                collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelectJoinRoutes(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
            if (!lastRoutePerformanceTotalsCriteria.equals(criteria))
            {
                collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelectJoinRoutes(criteria);
            }
        }
        lastRoutePerformanceTotalsCriteria = criteria;

        return collRoutePerformanceTotals;
    }









    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Routes is new, it will return
     * an empty collection; or if this Routes has previously
     * been saved, it will retrieve related RoutePerformanceTotals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Routes.
     */
    protected List getRoutePerformanceTotalsJoinDataSources(Criteria criteria)
        throws TorqueException
    {
        if (collRoutePerformanceTotals == null)
        {
            if (isNew())
            {
               collRoutePerformanceTotals = new ArrayList();
            }
            else
            {
                criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
                collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelectJoinDataSources(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            criteria.add(RoutePerformanceTotalPeer.ROUTE_ID, getId());
            if (!lastRoutePerformanceTotalsCriteria.equals(criteria))
            {
                collRoutePerformanceTotals = RoutePerformanceTotalPeer.doSelectJoinDataSources(criteria);
            }
        }
        lastRoutePerformanceTotalsCriteria = criteria;

        return collRoutePerformanceTotals;
    }



        
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
            fieldNames.add("Id");
            fieldNames.add("ProjectId");
            fieldNames.add("Name");
            fieldNames.add("Created");
            fieldNames.add("Modified");
            fieldNames.add("CreatedBy");
            fieldNames.add("ModifiedBy");
            fieldNames.add("Modstamp");
            fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by field (Java) name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
        if (name.equals("Id"))
        {
            return getId();
        }
        if (name.equals("ProjectId"))
        {
            return getProjectId();
        }
        if (name.equals("Name"))
        {
            return getName();
        }
        if (name.equals("Created"))
        {
            return getCreated();
        }
        if (name.equals("Modified"))
        {
            return getModified();
        }
        if (name.equals("CreatedBy"))
        {
            return getCreatedBy();
        }
        if (name.equals("ModifiedBy"))
        {
            return getModifiedBy();
        }
        if (name.equals("Modstamp"))
        {
            return getModstamp();
        }
        return null;
    }

    /**
     * Set a field in the object by field (Java) name.
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occurs with the set[Field] method.
     */
    public boolean setByName(String name, Object value )
        throws TorqueException, IllegalArgumentException
    {
        if (name.equals("Id"))
        {
            // Object fields can be null
            if (value != null && ! Long.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setId((Long) value);
            return true;
        }
        if (name.equals("ProjectId"))
        {
            // Object fields can be null
            if (value != null && ! Long.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setProjectId((Long) value);
            return true;
        }
        if (name.equals("Name"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setName((String) value);
            return true;
        }
        if (name.equals("Created"))
        {
            // Object fields can be null
            if (value != null && ! Date.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setCreated((Date) value);
            return true;
        }
        if (name.equals("Modified"))
        {
            // Object fields can be null
            if (value != null && ! Date.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setModified((Date) value);
            return true;
        }
        if (name.equals("CreatedBy"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setCreatedBy((String) value);
            return true;
        }
        if (name.equals("ModifiedBy"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setModifiedBy((String) value);
            return true;
        }
        if (name.equals("Modstamp"))
        {
            // Object fields can be null
            if (value != null && ! Date.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setModstamp((Date) value);
            return true;
        }
        return false;
    }

    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
        if (name.equals(RoutesPeer.ID))
        {
            return getId();
        }
        if (name.equals(RoutesPeer.PROJECT_ID))
        {
            return getProjectId();
        }
        if (name.equals(RoutesPeer.NAME))
        {
            return getName();
        }
        if (name.equals(RoutesPeer.CREATED))
        {
            return getCreated();
        }
        if (name.equals(RoutesPeer.MODIFIED))
        {
            return getModified();
        }
        if (name.equals(RoutesPeer.CREATED_BY))
        {
            return getCreatedBy();
        }
        if (name.equals(RoutesPeer.MODIFIED_BY))
        {
            return getModifiedBy();
        }
        if (name.equals(RoutesPeer.MODSTAMP))
        {
            return getModstamp();
        }
        return null;
    }

    /**
     * Set field values by Peer Field Name
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occurs with the set[Field] method.
     */
    public boolean setByPeerName(String name, Object value)
        throws TorqueException, IllegalArgumentException
    {
      if (RoutesPeer.ID.equals(name))
        {
            return setByName("Id", value);
        }
      if (RoutesPeer.PROJECT_ID.equals(name))
        {
            return setByName("ProjectId", value);
        }
      if (RoutesPeer.NAME.equals(name))
        {
            return setByName("Name", value);
        }
      if (RoutesPeer.CREATED.equals(name))
        {
            return setByName("Created", value);
        }
      if (RoutesPeer.MODIFIED.equals(name))
        {
            return setByName("Modified", value);
        }
      if (RoutesPeer.CREATED_BY.equals(name))
        {
            return setByName("CreatedBy", value);
        }
      if (RoutesPeer.MODIFIED_BY.equals(name))
        {
            return setByName("ModifiedBy", value);
        }
      if (RoutesPeer.MODSTAMP.equals(name))
        {
            return setByName("Modstamp", value);
        }
        return false;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
        if (pos == 0)
        {
            return getId();
        }
        if (pos == 1)
        {
            return getProjectId();
        }
        if (pos == 2)
        {
            return getName();
        }
        if (pos == 3)
        {
            return getCreated();
        }
        if (pos == 4)
        {
            return getModified();
        }
        if (pos == 5)
        {
            return getCreatedBy();
        }
        if (pos == 6)
        {
            return getModifiedBy();
        }
        if (pos == 7)
        {
            return getModstamp();
        }
        return null;
    }

    /**
     * Set field values by its position (zero based) in the XML schema.
     *
     * @param position The field position
     * @param value field value
     * @return True if value was set, false if not (invalid position / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occurs with the set[Field] method.
     */
    public boolean setByPosition(int position, Object value)
        throws TorqueException, IllegalArgumentException
    {
    if (position == 0)
        {
            return setByName("Id", value);
        }
    if (position == 1)
        {
            return setByName("ProjectId", value);
        }
    if (position == 2)
        {
            return setByName("Name", value);
        }
    if (position == 3)
        {
            return setByName("Created", value);
        }
    if (position == 4)
        {
            return setByName("Modified", value);
        }
    if (position == 5)
        {
            return setByName("CreatedBy", value);
        }
    if (position == 6)
        {
            return setByName("ModifiedBy", value);
        }
    if (position == 7)
        {
            return setByName("Modstamp", value);
        }
        return false;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
        save(RoutesPeer.DATABASE_NAME);
    }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
     *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
        try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
    }

    /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
        if (!alreadyInSave)
        {
            alreadyInSave = true;



            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    RoutesPeer.doInsert((Routes) this, con);
                    setNew(false);
                }
                else
                {
                    RoutesPeer.doUpdate((Routes) this, con);
                }
            }


            if (collRouteLinkss != null)
            {
                for (int i = 0; i < collRouteLinkss.size(); i++)
                {
                    ((RouteLinks) collRouteLinkss.get(i)).save(con);
                }
            }

            if (collRouteSetss != null)
            {
                for (int i = 0; i < collRouteSetss.size(); i++)
                {
                    ((RouteSets) collRouteSetss.get(i)).save(con);
                }
            }

            if (collRoutePerformanceTotals != null)
            {
                for (int i = 0; i < collRoutePerformanceTotals.size(); i++)
                {
                    ((RoutePerformanceTotal) collRoutePerformanceTotals.get(i)).save(con);
                }
            }
            alreadyInSave = false;
        }
    }


    /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key id ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
        setId(new Long(((NumberKey) key).longValue()));
    }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
        setId(new Long(key));
    }


    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
        return SimpleKey.keyFor(getId());
    }
 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     */
    public Routes copy() throws TorqueException
    {
        return copy(true);
    }

    /**
     * Makes a copy of this object using connection.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     *
     * @param con the database connection to read associated objects.
     */
    public Routes copy(Connection con) throws TorqueException
    {
        return copy(true, con);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     */
    public Routes copy(boolean deepcopy) throws TorqueException
    {
        return copyInto(new Routes(), deepcopy);
    }

    /**
     * Makes a copy of this object using connection.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     * @param con the database connection to read associated objects.
     */
    public Routes copy(boolean deepcopy, Connection con) throws TorqueException
    {
        return copyInto(new Routes(), deepcopy, con);
    }
  
    /**
     * Fills the copyObj with the contents of this object.
     * The associated objects are also copied and treated as new objects.
     *
     * @param copyObj the object to fill.
     */
    protected Routes copyInto(Routes copyObj) throws TorqueException
    {
        return copyInto(copyObj, true);
    }

  
    /**
     * Fills the copyObj with the contents of this object using connection.
     * The associated objects are also copied and treated as new objects.
     *
     * @param copyObj the object to fill.
     * @param con the database connection to read associated objects.
     */
    protected Routes copyInto(Routes copyObj, Connection con) throws TorqueException
    {
        return copyInto(copyObj, true, con);
    }
  
    /**
     * Fills the copyObj with the contents of this object.
     * If deepcopy is true, The associated objects are also copied
     * and treated as new objects.
     *
     * @param copyObj the object to fill.
     * @param deepcopy whether the associated objects should be copied.
     */
    protected Routes copyInto(Routes copyObj, boolean deepcopy) throws TorqueException
    {
        copyObj.setId(id);
        copyObj.setProjectId(projectId);
        copyObj.setName(name);
        copyObj.setCreated(created);
        copyObj.setModified(modified);
        copyObj.setCreatedBy(createdBy);
        copyObj.setModifiedBy(modifiedBy);
        copyObj.setModstamp(modstamp);

        copyObj.setId((Long)null);

        if (deepcopy)
        {


        List vRouteLinkss = getRouteLinkss();
        if (vRouteLinkss != null)
        {
            for (int i = 0; i < vRouteLinkss.size(); i++)
            {
                RouteLinks obj = (RouteLinks) vRouteLinkss.get(i);
                copyObj.addRouteLinks(obj.copy());
            }
        }
        else
        {
            copyObj.collRouteLinkss = null;
        }


        List vRouteSetss = getRouteSetss();
        if (vRouteSetss != null)
        {
            for (int i = 0; i < vRouteSetss.size(); i++)
            {
                RouteSets obj = (RouteSets) vRouteSetss.get(i);
                copyObj.addRouteSets(obj.copy());
            }
        }
        else
        {
            copyObj.collRouteSetss = null;
        }


        List vRoutePerformanceTotals = getRoutePerformanceTotals();
        if (vRoutePerformanceTotals != null)
        {
            for (int i = 0; i < vRoutePerformanceTotals.size(); i++)
            {
                RoutePerformanceTotal obj = (RoutePerformanceTotal) vRoutePerformanceTotals.get(i);
                copyObj.addRoutePerformanceTotal(obj.copy());
            }
        }
        else
        {
            copyObj.collRoutePerformanceTotals = null;
        }
        }
        return copyObj;
    }
        
    
    /**
     * Fills the copyObj with the contents of this object using connection.
     * If deepcopy is true, The associated objects are also copied
     * and treated as new objects.
     *
     * @param copyObj the object to fill.
     * @param deepcopy whether the associated objects should be copied.
     * @param con the database connection to read associated objects.
     */
    protected Routes copyInto(Routes copyObj, boolean deepcopy, Connection con) throws TorqueException
    {
        copyObj.setId(id);
        copyObj.setProjectId(projectId);
        copyObj.setName(name);
        copyObj.setCreated(created);
        copyObj.setModified(modified);
        copyObj.setCreatedBy(createdBy);
        copyObj.setModifiedBy(modifiedBy);
        copyObj.setModstamp(modstamp);

        copyObj.setId((Long)null);

        if (deepcopy)
        {


        List vRouteLinkss = getRouteLinkss(con);
        if (vRouteLinkss != null)
        {
            for (int i = 0; i < vRouteLinkss.size(); i++)
            {
                RouteLinks obj = (RouteLinks) vRouteLinkss.get(i);
                copyObj.addRouteLinks(obj.copy(con), con);
            }
        }
        else
        {
            copyObj.collRouteLinkss = null;
        }


        List vRouteSetss = getRouteSetss(con);
        if (vRouteSetss != null)
        {
            for (int i = 0; i < vRouteSetss.size(); i++)
            {
                RouteSets obj = (RouteSets) vRouteSetss.get(i);
                copyObj.addRouteSets(obj.copy(con), con);
            }
        }
        else
        {
            copyObj.collRouteSetss = null;
        }


        List vRoutePerformanceTotals = getRoutePerformanceTotals(con);
        if (vRoutePerformanceTotals != null)
        {
            for (int i = 0; i < vRoutePerformanceTotals.size(); i++)
            {
                RoutePerformanceTotal obj = (RoutePerformanceTotal) vRoutePerformanceTotals.get(i);
                copyObj.addRoutePerformanceTotal(obj.copy(con), con);
            }
        }
        else
        {
            copyObj.collRoutePerformanceTotals = null;
        }
        }
        return copyObj;
    }
    
    

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public RoutesPeer getPeer()
    {
        return peer;
    }

    /**
     * Retrieves the TableMap object related to this Table data without
     * compiler warnings of using getPeer().getTableMap().
     *
     * @return The associated TableMap object.
     */
    public TableMap getTableMap() throws TorqueException
    {
        return RoutesPeer.getTableMap();
    }


    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("Routes:\n");
        str.append("Id = ")
           .append(getId())
           .append("\n");
        str.append("ProjectId = ")
           .append(getProjectId())
           .append("\n");
        str.append("Name = ")
           .append(getName())
           .append("\n");
        str.append("Created = ")
           .append(getCreated())
           .append("\n");
        str.append("Modified = ")
           .append(getModified())
           .append("\n");
        str.append("CreatedBy = ")
           .append(getCreatedBy())
           .append("\n");
        str.append("ModifiedBy = ")
           .append(getModifiedBy())
           .append("\n");
        str.append("Modstamp = ")
           .append(getModstamp())
           .append("\n");
        return(str.toString());
    }
}

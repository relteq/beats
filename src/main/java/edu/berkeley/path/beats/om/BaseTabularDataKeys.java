package com.relteq.sirius.om;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Date;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;





/**
 * This class was autogenerated by Torque on:
 *
 * [Wed Nov 14 14:25:09 PST 2012]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to TabularDataKeys
 */
public abstract class BaseTabularDataKeys extends com.relteq.sirius.db.BaseObject
{
    /** Serial version */
    private static final long serialVersionUID = 1352931909099L;

    /** The Peer class */
    private static final TabularDataKeysPeer peer =
        new TabularDataKeysPeer();


    /** The value for the tableId field */
    private Long tableId;

    /** The value for the columnName field */
    private String columnName;

    /** The value for the columnNumber field */
    private Integer columnNumber;

    /** The value for the isKey field */
    private Boolean isKey;


    /**
     * Get the TableId
     *
     * @return Long
     */
    public Long getTableId()
    {
        return tableId;
    }


    /**
     * Set the value of TableId
     *
     * @param v new value
     */
    public void setTableId(Long v) throws TorqueException
    {

        if (!ObjectUtils.equals(this.tableId, v))
        {
            this.tableId = v;
            setModified(true);
        }


        if (aTables != null && !ObjectUtils.equals(aTables.getId(), v))
        {
            aTables = null;
        }

    }

    /**
     * Get the ColumnName
     *
     * @return String
     */
    public String getColumnName()
    {
        return columnName;
    }


    /**
     * Set the value of ColumnName
     *
     * @param v new value
     */
    public void setColumnName(String v) 
    {

        if (!ObjectUtils.equals(this.columnName, v))
        {
            this.columnName = v;
            setModified(true);
        }


    }

    /**
     * Get the ColumnNumber
     *
     * @return Integer
     */
    public Integer getColumnNumber()
    {
        return columnNumber;
    }


    /**
     * Set the value of ColumnNumber
     *
     * @param v new value
     */
    public void setColumnNumber(Integer v) 
    {

        if (!ObjectUtils.equals(this.columnNumber, v))
        {
            this.columnNumber = v;
            setModified(true);
        }


    }

    /**
     * Get the IsKey
     *
     * @return Boolean
     */
    public Boolean getIsKey()
    {
        return isKey;
    }


    /**
     * Set the value of IsKey
     *
     * @param v new value
     */
    public void setIsKey(Boolean v) 
    {

        if (!ObjectUtils.equals(this.isKey, v))
        {
            this.isKey = v;
            setModified(true);
        }


    }

    



    private Tables aTables;

    /**
     * Declares an association between this object and a Tables object
     *
     * @param v Tables
     * @throws TorqueException
     */
    public void setTables(Tables v) throws TorqueException
    {
        if (v == null)
        {
            setTableId((Long) null);
        }
        else
        {
            setTableId(v.getId());
        }
        aTables = v;
    }


    /**
     * Returns the associated Tables object.
     * If it was not retrieved before, the object is retrieved from
     * the database
     *
     * @return the associated Tables object
     * @throws TorqueException
     */
    public Tables getTables()
        throws TorqueException
    {
        if (aTables == null && (!ObjectUtils.equals(this.tableId, null)))
        {
            aTables = TablesPeer.retrieveByPK(SimpleKey.keyFor(this.tableId));
        }
        return aTables;
    }

    /**
     * Return the associated Tables object
     * If it was not retrieved before, the object is retrieved from
     * the database using the passed connection
     *
     * @param connection the connection used to retrieve the associated object
     *        from the database, if it was not retrieved before
     * @return the associated Tables object
     * @throws TorqueException
     */
    public Tables getTables(Connection connection)
        throws TorqueException
    {
        if (aTables == null && (!ObjectUtils.equals(this.tableId, null)))
        {
            aTables = TablesPeer.retrieveByPK(SimpleKey.keyFor(this.tableId), connection);
        }
        return aTables;
    }

    /**
     * Provides convenient way to set a relationship based on a
     * ObjectKey, for example
     * <code>bar.setFooKey(foo.getPrimaryKey())</code>
     *
     */
    public void setTablesKey(ObjectKey key) throws TorqueException
    {

        setTableId(new Long(((NumberKey) key).longValue()));
    }
   
        
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
            fieldNames.add("TableId");
            fieldNames.add("ColumnName");
            fieldNames.add("ColumnNumber");
            fieldNames.add("IsKey");
            fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by field (Java) name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
        if (name.equals("TableId"))
        {
            return getTableId();
        }
        if (name.equals("ColumnName"))
        {
            return getColumnName();
        }
        if (name.equals("ColumnNumber"))
        {
            return getColumnNumber();
        }
        if (name.equals("IsKey"))
        {
            return getIsKey();
        }
        return null;
    }

    /**
     * Set a field in the object by field (Java) name.
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occurs with the set[Field] method.
     */
    public boolean setByName(String name, Object value )
        throws TorqueException, IllegalArgumentException
    {
        if (name.equals("TableId"))
        {
            // Object fields can be null
            if (value != null && ! Long.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setTableId((Long) value);
            return true;
        }
        if (name.equals("ColumnName"))
        {
            // Object fields can be null
            if (value != null && ! String.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setColumnName((String) value);
            return true;
        }
        if (name.equals("ColumnNumber"))
        {
            // Object fields can be null
            if (value != null && ! Integer.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setColumnNumber((Integer) value);
            return true;
        }
        if (name.equals("IsKey"))
        {
            // Object fields can be null
            if (value != null && ! Boolean.class.isInstance(value))
            {
                throw new IllegalArgumentException("Invalid type of object specified for value in setByName");
            }
            setIsKey((Boolean) value);
            return true;
        }
        return false;
    }

    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
        if (name.equals(TabularDataKeysPeer.TABLE_ID))
        {
            return getTableId();
        }
        if (name.equals(TabularDataKeysPeer.COLUMN_NAME))
        {
            return getColumnName();
        }
        if (name.equals(TabularDataKeysPeer.COLUMN_NUMBER))
        {
            return getColumnNumber();
        }
        if (name.equals(TabularDataKeysPeer.IS_KEY))
        {
            return getIsKey();
        }
        return null;
    }

    /**
     * Set field values by Peer Field Name
     *
     * @param name field name
     * @param value field value
     * @return True if value was set, false if not (invalid name / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occurs with the set[Field] method.
     */
    public boolean setByPeerName(String name, Object value)
        throws TorqueException, IllegalArgumentException
    {
      if (TabularDataKeysPeer.TABLE_ID.equals(name))
        {
            return setByName("TableId", value);
        }
      if (TabularDataKeysPeer.COLUMN_NAME.equals(name))
        {
            return setByName("ColumnName", value);
        }
      if (TabularDataKeysPeer.COLUMN_NUMBER.equals(name))
        {
            return setByName("ColumnNumber", value);
        }
      if (TabularDataKeysPeer.IS_KEY.equals(name))
        {
            return setByName("IsKey", value);
        }
        return false;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
        if (pos == 0)
        {
            return getTableId();
        }
        if (pos == 1)
        {
            return getColumnName();
        }
        if (pos == 2)
        {
            return getColumnNumber();
        }
        if (pos == 3)
        {
            return getIsKey();
        }
        return null;
    }

    /**
     * Set field values by its position (zero based) in the XML schema.
     *
     * @param position The field position
     * @param value field value
     * @return True if value was set, false if not (invalid position / protected field).
     * @throws IllegalArgumentException if object type of value does not match field object type.
     * @throws TorqueException If a problem occurs with the set[Field] method.
     */
    public boolean setByPosition(int position, Object value)
        throws TorqueException, IllegalArgumentException
    {
    if (position == 0)
        {
            return setByName("TableId", value);
        }
    if (position == 1)
        {
            return setByName("ColumnName", value);
        }
    if (position == 2)
        {
            return setByName("ColumnNumber", value);
        }
    if (position == 3)
        {
            return setByName("IsKey", value);
        }
        return false;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
        save(TabularDataKeysPeer.DATABASE_NAME);
    }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
     *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
        try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
    }

    /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
        if (!alreadyInSave)
        {
            alreadyInSave = true;



            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    TabularDataKeysPeer.doInsert((TabularDataKeys) this, con);
                    setNew(false);
                }
                else
                {
                    TabularDataKeysPeer.doUpdate((TabularDataKeys) this, con);
                }
            }

            alreadyInSave = false;
        }
    }



    private final SimpleKey[] pks = new SimpleKey[2];
    private final ComboKey comboPK = new ComboKey(pks);

    /**
     * Set the PrimaryKey with an ObjectKey
     *
     * @param key
     */
    public void setPrimaryKey(ObjectKey key) throws TorqueException
    {
        SimpleKey[] keys = (SimpleKey[]) key.getValue();
        setTableId(new Long(((NumberKey)keys[0]).longValue()));
        setColumnName(keys[1].toString());
    }

    /**
     * Set the PrimaryKey using SimpleKeys.
     *
     * @param tableId Long
     * @param columnName String
     */
    public void setPrimaryKey( Long tableId, String columnName)
        throws TorqueException
    {
        setTableId(tableId);
        setColumnName(columnName);
    }

    /**
     * Set the PrimaryKey using a String.
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
        setPrimaryKey(new ComboKey(key));
    }

    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
        pks[0] = SimpleKey.keyFor(getTableId());
        pks[1] = SimpleKey.keyFor(getColumnName());
        return comboPK;
    }
 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     */
    public TabularDataKeys copy() throws TorqueException
    {
        return copy(true);
    }

    /**
     * Makes a copy of this object using connection.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     *
     * @param con the database connection to read associated objects.
     */
    public TabularDataKeys copy(Connection con) throws TorqueException
    {
        return copy(true, con);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     */
    public TabularDataKeys copy(boolean deepcopy) throws TorqueException
    {
        return copyInto(new TabularDataKeys(), deepcopy);
    }

    /**
     * Makes a copy of this object using connection.
     * It creates a new object filling in the simple attributes.
     * If the parameter deepcopy is true, it then fills all the
     * association collections and sets the related objects to
     * isNew=true.
     *
     * @param deepcopy whether to copy the associated objects.
     * @param con the database connection to read associated objects.
     */
    public TabularDataKeys copy(boolean deepcopy, Connection con) throws TorqueException
    {
        return copyInto(new TabularDataKeys(), deepcopy, con);
    }
  
    /**
     * Fills the copyObj with the contents of this object.
     * The associated objects are also copied and treated as new objects.
     *
     * @param copyObj the object to fill.
     */
    protected TabularDataKeys copyInto(TabularDataKeys copyObj) throws TorqueException
    {
        return copyInto(copyObj, true);
    }

  
    /**
     * Fills the copyObj with the contents of this object using connection.
     * The associated objects are also copied and treated as new objects.
     *
     * @param copyObj the object to fill.
     * @param con the database connection to read associated objects.
     */
    protected TabularDataKeys copyInto(TabularDataKeys copyObj, Connection con) throws TorqueException
    {
        return copyInto(copyObj, true, con);
    }
  
    /**
     * Fills the copyObj with the contents of this object.
     * If deepcopy is true, The associated objects are also copied
     * and treated as new objects.
     *
     * @param copyObj the object to fill.
     * @param deepcopy whether the associated objects should be copied.
     */
    protected TabularDataKeys copyInto(TabularDataKeys copyObj, boolean deepcopy) throws TorqueException
    {
        copyObj.setTableId(tableId);
        copyObj.setColumnName(columnName);
        copyObj.setColumnNumber(columnNumber);
        copyObj.setIsKey(isKey);

        copyObj.setTableId((Long)null);
        copyObj.setColumnName((String)null);

        if (deepcopy)
        {
        }
        return copyObj;
    }
        
    
    /**
     * Fills the copyObj with the contents of this object using connection.
     * If deepcopy is true, The associated objects are also copied
     * and treated as new objects.
     *
     * @param copyObj the object to fill.
     * @param deepcopy whether the associated objects should be copied.
     * @param con the database connection to read associated objects.
     */
    protected TabularDataKeys copyInto(TabularDataKeys copyObj, boolean deepcopy, Connection con) throws TorqueException
    {
        copyObj.setTableId(tableId);
        copyObj.setColumnName(columnName);
        copyObj.setColumnNumber(columnNumber);
        copyObj.setIsKey(isKey);

        copyObj.setTableId((Long)null);
        copyObj.setColumnName((String)null);

        if (deepcopy)
        {
        }
        return copyObj;
    }
    
    

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public TabularDataKeysPeer getPeer()
    {
        return peer;
    }

    /**
     * Retrieves the TableMap object related to this Table data without
     * compiler warnings of using getPeer().getTableMap().
     *
     * @return The associated TableMap object.
     */
    public TableMap getTableMap() throws TorqueException
    {
        return TabularDataKeysPeer.getTableMap();
    }


    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("TabularDataKeys:\n");
        str.append("TableId = ")
           .append(getTableId())
           .append("\n");
        str.append("ColumnName = ")
           .append(getColumnName())
           .append("\n");
        str.append("ColumnNumber = ")
           .append(getColumnNumber())
           .append("\n");
        str.append("IsKey = ")
           .append(getIsKey())
           .append("\n");
        return(str.toString());
    }
}
